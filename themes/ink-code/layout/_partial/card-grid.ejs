<% var posts = posts || page.posts; %>
<% if (posts && posts.data && posts.data.length) { posts = posts.data; } %>
<% if (posts && posts.length) { %>
  <% for (var i = 0; i < posts.length; i++) { %>
    <%- partial('_partial/card', { post: posts[i], index: i }) %>
  <% } %>
<% } %>
